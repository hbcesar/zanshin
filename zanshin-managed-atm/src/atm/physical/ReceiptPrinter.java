/* * ATM Example system - file ReceiptPrinter.java * * copyright (c) 2001 - Russell C. Bjork * */package atm.physical;import java.util.Enumeration;import simulation.Simulation;import zanshin.TargetSystemController;import banking.Receipt;/** * Manager for the ATM's receipt printer. In a real ATM, this would manage a physical device; in this simulation, it * uses classes in package simulation to simulate the device. */public class ReceiptPrinter {	/**	 * Constructor	 */	public ReceiptPrinter() {}	/**	 * Print a receipt	 * 	 * @param receipt	 *          object containing the information to be printed	 */	public void printReceipt(Receipt receipt) {		// <Zanshin Modification>		// Simulating an error in the ATM in case the user chose a defective one.		TargetSystemController controller = TargetSystemController.getInstance();		controller.simulateErrorIfDefective();				Enumeration receiptLines = receipt.getLines();		// Animate the printing of the receipt		while (receiptLines.hasMoreElements()) {			Simulation.getInstance().printReceiptLine(((String) receiptLines.nextElement()));		}	}}